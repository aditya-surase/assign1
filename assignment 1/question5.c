#include <stdio.h>

int main() {
    int age;

    age = 20;   // given age

    if (age >= 18) {
        printf("Eligible to vote");
    } else {
        printf("Not eligible to vote");
    }

    return 0;
}